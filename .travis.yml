language: php

services:
  - mysql

php:
  - 5.5
  - 5.6

before_install:
  - rm composer.lock
  - composer self-update
  - npm -g install npm@next

install:
  - composer install
  - npm install

before_script:
  - mysql -e 'CREATE DATABASE `Traveling-Children-Project_Test`'

script:
  - gulp
  - php artisan --env=staging --force migrate:refresh --seed
