language: php

services:
  - mysql

php:
  - 5.5
  - 5.6

install:
  - rm composer.lock
  - composer self-update
  - composer install
  - npm -g install npm@next
  - npm install

before_script:
  - mysql -e 'CREATE DATABASE `Traveling-Children-Project_Development`'

script:
  - gulp
  - php artisan migrate:refresh --seed --force
